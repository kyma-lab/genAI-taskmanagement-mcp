# MCP Task Server - Environment Variables Template
# Copy this file to .env and fill in the values
# IMPORTANT: Never commit .env to version control!

# Database Configuration
DB_NAME=taskdb
DB_USER=taskuser
DB_PASSWORD=your-secure-password-here  # Change this!
DB_HOST=localhost
DB_PORT=5432

# CORS Configuration (HTTP transport only)
# Comma-separated list of allowed origins for the HTTP/SSE endpoint.
# Leave empty to allow all origins (only acceptable for private networks or local dev).
# For any internet-facing deployment set explicit origins to prevent CSRF/cross-origin attacks.
# Example: MCP_CORS_ORIGINS=http://localhost:3000,https://myapp.com
MCP_CORS_ORIGINS=
