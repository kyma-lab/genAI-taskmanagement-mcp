spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
  flyway:
    enabled: false

security:
  api-key:
    enabled: false

logging:
  level:
    root: WARN
    com.example.mcptaskserver: INFO

mcp:
  transport:
    mode: http  # Disable STDIO server in tests to prevent blocking
